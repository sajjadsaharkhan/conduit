/** Application branding — single source of truth for name and tagline */
export const APP_NAME = 'Conduit'
export const APP_TAGLINE = 'Proxy Client'
export const APP_TITLE = `${APP_NAME} — ${APP_TAGLINE}`
